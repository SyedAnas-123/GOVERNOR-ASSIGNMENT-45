let names: string[] = ["Alice", "Bob", "Charlie", "David"]; // Array storing names of friends

// Printing a personalized message to each person in the array
for (let i = 0; i < names.length; i++) {
    console.log(`Hello, ${names[i]}! How are you today?`);
}
