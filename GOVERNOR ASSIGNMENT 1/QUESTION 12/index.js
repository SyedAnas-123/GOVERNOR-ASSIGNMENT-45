var names = ["Alice", "Bob", "Charlie", "David"]; // Array storing names of friends
// Printing each person's name by accessing each element in the list
for (var i = 0; i < names.length; i++) {
    console.log(names[i]);
}
