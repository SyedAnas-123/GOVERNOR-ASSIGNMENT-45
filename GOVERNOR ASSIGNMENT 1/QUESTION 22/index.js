var numbers = [1, 2, 3, 4, 5];
// Intentional error: Accessing an index that doesn't exist
console.log(numbers[10]); // This will produce an index error
// Correcting the error
// Accessing an index within the range of the array
console.log(numbers[2]); // This will print 3, which is the value at index 2
