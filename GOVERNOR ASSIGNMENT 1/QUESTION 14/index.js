var transportationModes = ["motorcycle", "car", "bicycle", "scooter"];
// Printing statements about each item in the transportationModes array
for (var i = 0; i < transportationModes.length; i++) {
    console.log("I would like to own a ".concat(transportationModes[i], "."));
}
